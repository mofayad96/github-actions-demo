on: 
  workflow_dispatch: # Gui or CLI
    inputs:
      environment:
        description: "choose environment"
        required: true
        default: "staging"
        type: choice
        options:
          - staging
          - production
jobs:
  deploy: 
    runs-on: ubuntu-latest
    steps:
      - name: deploy app
        run: echo "deploying the application...."
